# Build

One draft PR. Honor the monthly mix.

1. If Incubator is full, only `work:improve`.
2. If the e2e loop is not green yet, only work on `foundry` (and the pypi specimen panel). After one green loop, one new utility is allowed.
3. Implement DoD, attach measure, open draft PR.
4. Stop.
